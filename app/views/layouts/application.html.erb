<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <link type="text/css" media="screen" rel="stylesheet" href="responsive-tables.css" />
    <script type="text/javascript" src="responsive-tables.js"></script>
  </head>

  <body>

    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="greeting">
        </li>
      </ul>
      <section class="top-bar-section">
        <ul class="left">
          <% if user_signed_in? %>
           <li class="has-dropdown">
            <a href="#">Profile</a>
              <ul class="dropdown">
                <li><%= link_to 'Edit Login Credentials', edit_user_registration_path(current_user) %></li>
                <li><%= link_to 'Profile', user_path(current_user) %></li>
              </ul>
            </li>
            <li class="has-dropdown">
            <a href="#">Activities</a>
              <ul class="dropdown">
                <li class="active"> <%= link_to 'View/Select from List', user_activities_path(current_user) %> </li>
              </ul>
            </li>
            <li class="has-dropdown">
            <a href="#">Events</a>
              <ul class="dropdown">
                <li class="active"> <%= link_to 'Create New Event', new_user_event_path(current_user) %> </li>
                <li class="active"> <%= link_to 'View Events', user_events_path(current_user) %> </li>
              </ul>
            </li>
          <% else %>
            <li class="active"><%= link_to 'Sign In', new_user_session_path %> </li>
            <li class="active"><%= link_to 'Sign Up Here', new_user_registration_path %></li>
            <li class="active"><%= link_to 'Contact Us', new_contact_path %></li>
          <% end %>
        </ul>
        <ul class="right">
          <% if user_signed_in? %>
            <li class="active"> <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %> </li>
          <% end %>
        </ul>
      </section>
    </nav>
    <% if user_signed_in? %>
      <strong> Hello, </strong> <%= current_user.first_name %>
    <% end %>

    <p class="notice"> <%= notice %> </p>
    <p class="alert"> <%= alert %> </p>

  <div class="row">
    <div class="small-12 columns">
      <%= yield %>
    </div>
  </div>

<footer class="row">
  <div class="large-12 columns">
    <hr>
    <div class="small-3 columns">
      <%= link_to 'Contact Us', new_contact_path %>
    </div>
    <div class="small-6 columns">
      <p> Simple Social </p>
    </div>
    <div class="small-3 columns">
      <%= link_to 'About', pages_path %>
    </div>
  </div>
</footer>
    <%= javascript_include_tag "application" %>
  </body>
</html>
